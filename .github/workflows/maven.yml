# This workflow will build a Java project with Maven
# For more information see: https://help.github.com/actions/language-and-framework-guides/building-and-testing-java-with-maven

name: Zipping and Moving the Folder

on:
  push:
    branches: [ main ]
 

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    #Checkout the Code
    - name: Step-1 Checkout the code
      uses: actions/checkout@v2
      
    - name: Build maven
      run: mvn clean install
        
  #zip the file  
    - name: Step-3 Zip the Code
      uses: papeloto/action-zip@v1
      with:
          dest: result.zip
    - name: check path
      run: ls -a;
          
 #Velify the all the folders in Main Directory     
    - name: Operations
      uses: fifsky/ssh-action@master
      with:
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          host: 18.219.65.137
          port: 22
          user: ec2-user 
          command: 
                  sudo su;
                  ls -a;
                
    - name: copy file via ssh password
      uses: appleboy/scp-action@master
      with:
        host: 18.219.65.137
        username: ec2-user
        password: ${{ secrets.SSH_KEY }}
        port: 22
        source: "/home/runner/work/Github-actions-demo/Github-actions-demo/target/SubmissionDataForm-0.0.1-SNAPSHOT.war"
        target: "/home/ec2-user/Actions_Git/"
    - name: Check path
      run: 
        cd .. ;
        cd .. ;
        ls -a

