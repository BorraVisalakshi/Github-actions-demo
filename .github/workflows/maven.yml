# This workflow will build a Java project with Maven
# For more information see: https://help.github.com/actions/language-and-framework-guides/building-and-testing-java-with-maven

name: Zipping and Moving the Folder

on:
  push:
    branches: [ main ]
 

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    #Checkout the Code
    - name: Step-1 Checkout the code
      uses: actions/checkout@v2
        
  #zip the file  
    - name: Step-3 Zip the Code
      uses: papeloto/action-zip@v1
      with:
          dest: result.zip
          
 #Velify the all the folders in Main Directory     
    - name: Check the folder
      run: dir
    - name: Copy single file to remote
      uses: garygrossgarten/github-action-scp@release
      with:
          local: /home/runner/work/Github-actions-demo/Github-actions-demo/target/SubmissionDataForm-0.0.1-SNAPSHOT.war
          remote: scp/single/SubmissionDataForm-0.0.1-SNAPSHOT.war
          host: 3.143.68.173
          username: ec2-user
          password: aws123
          privateKey: ${{ secrets.SSH_KEY}}

